{
    "configurations": [
    {
        "name": "(lldb) launch",
        "type": "cppdbg",
        "request": "launch",
        "program": "${workspaceFolder}/a.out",
        "MIMode": "lldb",
        "cwd": "${workspaceFolder}"
    }
    ],
}